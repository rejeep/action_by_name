Simple example of how to use ActionByName. To make this work, copy the two classes ActionController and ApplicationController to this folder.

Simply run ./run.sh (if you use javac compiler) in a terminal to compile and run.